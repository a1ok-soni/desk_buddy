[project]
name = "desk-buddy"
version = "0.1.0"
description = ""
authors = [
    {name = "a1ok-soni",email = "aaloksoni.alok@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "ollama (>=0.6.2,<0.7.0)",
    "pandas (>=3.0.5,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [{ include = "buddy" }]
